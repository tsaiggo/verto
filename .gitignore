# dependencies
/node_modules
/.pnp
.pnp.*

# testing
/coverage

# next.js
/.next/
/out/

# excalidraw assets vendored from node_modules (see scripts/copy-excalidraw-assets.mjs)
/public/excalidraw-assets/

# tauri build artifacts
/src-tauri/target/
/src-tauri/gen/schemas

# production
/build

# misc
.DS_Store
.idea/
*.pem

# impeccable design-hook cache (local tooling state)
/.impeccable/

# agent scaffolding (local coding-agent state: plans, goals, run continuation)
/.omo/
/.goals/

# design mockups — large local reference art (84MB of PNGs), not tracked in git.
# Kept locally as the source-of-truth for the UI redesign; use Git LFS if these
# ever need to be versioned.
/docs/design/

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Playwright browser test artifacts
/test-results/
